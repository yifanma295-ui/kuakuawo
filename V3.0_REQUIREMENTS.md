# "夸夸我" App V3.0 优化需求文档

## 项目背景
基于 V2.0 版本（已发布 H5 内测版），根据用户反馈和内测数据，进行深度优化迭代。

---

## 优化需求清单

### 1. 昵称全局同步 🎯
**优先级**：🔴 高  
**问题描述**：用户在 Onboarding 界面输入昵称后，仅在首页显示，其他页面（Echo 页面、AI 生成文案等）仍显示默认的"朋友"。

**需求**：
- Onboarding 完成后，所有页面的昵称都应实时更新为用户输入的昵称
- 包括但不限于：
  - 首页顶部标题（当前："朋友"）
  - AI 生成的文案中的昵称引用
  - Echo 页面的昵称显示
  - 管理后台的用户昵称记录

**实现建议**：
- 在 `app-context.tsx` 中确保昵称状态全局共享
- 所有依赖昵称的组件通过 `useApp()` Hook 获取最新值
- 测试场景：修改昵称后刷新各个页面，验证同步效果

---

### 2. 昵称编辑功能 ✏️
**优先级**：🔴 高  
**问题描述**：用户设置昵称后无法修改，需要提供便捷的编辑入口。

**需求**：
- 在首页昵称后面添加编辑图标（铅笔 icon）
- 在 Echo 页面昵称后面添加编辑图标（铅笔 icon）
- 点击编辑图标弹出昵称修改弹窗
- 支持用户随时修改昵称，修改后立即全局同步

**交互流程**：
1. 用户点击昵称旁的编辑图标
2. 弹出 Modal 或 BottomSheet，显示输入框和当前昵称
3. 用户修改昵称并确认
4. 昵称立即更新，所有页面同步

**设计建议**：
- 编辑图标使用 Material Icons 中的 `edit` 或 `pencil`
- 图标大小：16-18px，放在昵称右侧，颜色与昵称文字相同
- 弹窗样式与 Onboarding 界面保持一致

---

### 3. 文案生成加速 ⚡
**优先级**：🔴 高  
**问题描述**：当前 AI 文案生成速度过慢（用户反馈"太慢太慢了"），严重影响用户体验。

**需求**：
- 优化 DeepSeek API 调用性能
- 目标：将生成时间从当前的 3-5 秒降低到 1-2 秒以内

**优化方向**：
1. **Prompt 优化**：
   - 简化 Prompt，减少不必要的上下文
   - 移除冗长的示例，使用更简洁的指令
   - 明确指定输出格式，避免模型生成额外内容

2. **API 参数优化**：
   - 调整 `temperature` 参数（当前值？）→ 推荐 0.7-0.8
   - 调整 `max_tokens` 参数（当前值？）→ 推荐 150-200
   - 尝试 `top_p` 参数优化采样策略

3. **前端优化**：
   - 添加加载状态指示（Loading Spinner 或 Skeleton）
   - 考虑使用流式输出（Streaming）显示逐字生成过程
   - 实现请求超时控制（如 10 秒无响应则提示用户）

4. **缓存策略**（可选）：
   - 缓存相同主题 + 相同用户输入的生成结果
   - 使用 LocalStorage 或 AsyncStorage 存储最近的生成结果

**验证方式**：
- 在管理后台记录每次生成的耗时
- 收集用户反馈，确认加速效果

---

### 4. 背景渐变优化 🎨
**优先级**：🟡 中  
**问题描述**：当前背景色为静态的奶黄色到淡橘粉渐变，缺乏动态感。

**需求**：
- 背景色调整为：**淡蓝色 → 橘黄色 → 淡粉色** 的三色渐变
- 背景需要**浮动动画**，营造呼吸感
- 动画应该温和、舒适，不刺眼

**设计建议**：
1. **颜色方案**：
   - 淡蓝色：`#E0F2FE` 或 `#B3E5FC`
   - 橘黄色：`#FFE0B2` 或 `#FFCC80`
   - 淡粉色：`#FCE4EC` 或 `#F8BBD0`
   - 渐变方向：从左上到右下（135°）或从上到下（180°）

2. **浮动动画**：
   - 使用 `react-native-reanimated` 实现
   - 动画时长：4-6 秒（缓慢、舒适）
   - 效果：背景色位置轻微移动，营造"呼吸"感
   - 示例：`translateY` 或 `scale` 的微小变化（±5-10%）

3. **实现位置**：
   - 在 `ScreenContainer` 或各页面的 `LinearGradient` 中应用
   - 确保所有页面（首页、主题页、Echo 页面）保持一致

**参考代码框架**：
```tsx
// 使用 react-native-reanimated + LinearGradient
<Animated.View style={animatedStyle}>
  <LinearGradient
    colors={["#E0F2FE", "#FFE0B2", "#FCE4EC"]}
    locations={[0, 0.5, 1]}
    style={StyleSheet.absoluteFillObject}
  />
</Animated.View>
```

---

### 5. 中间圆形图标升级 🌊
**优先级**：🟡 中  
**问题描述**：当前首页中间的圆形图标（心形）样式单调，视觉效果不佳。

**需求**：
- 升级为**浮动的渐变圆形**
- 添加**水波纹呼吸感**动画
- 增强整体视觉层次感

**设计建议**：
1. **圆形样式**：
   - 使用渐变色填充（推荐：粉红色 → 橘黄色 或 紫色 → 粉色）
   - 圆形大小：当前大小基础上可适度调整
   - 添加阴影效果，增强立体感

2. **浮动动画**：
   - 垂直浮动：`translateY` 循环动画，幅度 ±10-15px
   - 时长：3-4 秒，无限循环
   - 缓动函数：`Easing.inOut(Easing.ease)` 或 `Easing.bezier()`

3. **水波纹呼吸感**：
   - 在圆形外层添加 2-3 层同心圆
   - 每层圆形的 `scale` 和 `opacity` 循环变化
   - 效果：从中心向外扩散的波纹
   - 时长：2-3 秒，错开延迟实现连续波纹

**参考代码框架**：
```tsx
// 浮动 + 水波纹效果
<Animated.View style={[floatingStyle, pulseStyle]}>
  <LinearGradient
    colors={["#FF69B4", "#FFB347"]}
    style={styles.circle}
  />
  {/* 波纹层 */}
  <Animated.View style={[styles.ripple, ripple1Style]} />
  <Animated.View style={[styles.ripple, ripple2Style]} />
</Animated.View>
```

---

### 6. AI 文案风格调试 🎭
**优先级**：🟡 中  
**问题描述**：当前 AI 生成的文案风格不满意，需要提供调试机制。

**问题分析**：
- 不同主题下的文案风格差异
- 用户输入时的 AI 文案生成逻辑
- 文案的情感度、长度、表达方式等

**需求**：
- 在管理后台提供 **Prompt 编辑界面**
- 支持实时调试 AI 文案生成逻辑
- 记录不同 Prompt 版本的生成结果，便于对比

**调试方向**（参考）：
1. **文案风格维度**：
   - 情感度：温暖 ↔ 理性
   - 长度：简短（1 句）↔ 详细（3-5 句）
   - 表达：直接 ↔ 隐喻
   - 语气：正式 ↔ 随意

2. **主题特定化**：
   - 每个主题（孤独感、小确幸、行动力等）是否需要不同的 Prompt？
   - 当前是否有主题特定的 Prompt？

3. **用户输入融合**：
   - 如何更好地融合用户输入内容到生成文案中？
   - 是否需要对用户输入进行情感分析？

**实现建议**：
- 在 `/admin` 页面新增 "Prompt 管理" 模块
- 显示当前使用的 Prompt 版本
- 提供快速测试功能：输入用户昵称 + 主题 → 生成预览
- 记录每个 Prompt 版本的创建时间和使用统计

---

### 7. Echo 页面布局优化 📱
**优先级**：🟡 中  
**问题描述**：Echo 页面顶部显示英文 "Echo" 和用户昵称，布局不够突出。

**需求**：
- **删除**顶部的英文 "Echo" 文字
- **放大并突出**用户昵称显示
- 保持整体视觉平衡

**设计建议**：
1. **昵称展示**：
   - 字体大小：从当前的 14px 增加到 24-28px
   - 字重：保持 700（Bold）
   - 颜色：保持当前的棕色系
   - 位置：居中或左对齐（根据设计稿）

2. **页面结构**：
   ```
   [用户昵称] ✏️ (编辑图标)
   ─────────────────
   与自己的共鸣，向世界的回响 (副标题)
   ─────────────────
   [共鸣] [回响] (统计卡片)
   ─────────────────
   [激励文案]
   ─────────────────
   [收藏列表]
   ```

3. **视觉调整**：
   - 移除或隐藏当前的小昵称显示
   - 确保编辑图标与昵称对齐
   - 保持与首页的视觉一致性

---

## 优先级排序

| 优先级 | 需求 | 预计工作量 |
|--------|------|----------|
| 🔴 高 | 1. 昵称全局同步 | 2-3 小时 |
| 🔴 高 | 2. 昵称编辑功能 | 2-3 小时 |
| 🔴 高 | 3. 文案生成加速 | 3-4 小时 |
| 🟡 中 | 4. 背景渐变优化 | 1-2 小时 |
| 🟡 中 | 5. 圆形图标升级 | 2-3 小时 |
| 🟡 中 | 6. AI 文案风格调试 | 2-3 小时 |
| 🟡 中 | 7. Echo 页面优化 | 1 小时 |

**建议实施顺序**：
1. 先完成 3 个高优先级需求（1、2、3）
2. 再进行 4 个中优先级需求（4、5、6、7）

---

## 测试验证清单

- [ ] 昵称修改后，所有页面同步更新
- [ ] 编辑图标在首页和 Echo 页面都可见且可点击
- [ ] 文案生成时间 < 2 秒
- [ ] 背景动画流畅，无卡顿
- [ ] 圆形图标浮动和波纹动画正常
- [ ] 管理后台可查看 Prompt 版本
- [ ] Echo 页面昵称显示突出，无 "Echo" 英文

---

## 附注

- 所有动画应在移动端和 Web 端都保持流畅
- 确保无障碍访问（Accessibility）
- 在低端设备上测试性能
- 收集用户反馈，持续优化
