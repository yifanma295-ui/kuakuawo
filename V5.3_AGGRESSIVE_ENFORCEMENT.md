# V5.3 激进强制：显式提取关键词，强制 AI 必须使用

## 问题诊断

### V5.2 的失败

**用户反馈：** "我验证测试了，都还是只有通用型的回复，没有提到用户具体输入的内容"

**根本原因：**
- V5.2 虽然强调了"必须回应具体内容"，但这只是"提醒"
- AI 仍然可以"理解"这个要求，但在生成时"忽略"它
- **"提醒"的强制性不够，需要更激进的策略**

---

## V5.3 核心策略：显式提取关键词

### 策略 1：在 User Prompt 中显式提取关键词

**V5.2 的做法（失败）：**
```
${nickname} 对你说：
"${userInput}"

**重要提醒：你必须在回应中明确提到"${userInput}"里的具体事情。**
```

**问题：**
- 只是"提醒"AI 要回应具体内容
- AI 可以"理解"但"不执行"

---

**V5.3 的做法（激进）：**
```
${nickname} 对你说：
"${userInput}"

【关键词提取】
从用户的输入中，我们提取了以下关键词：洗澡、狗狗

【强制要求】
1. 你的第一句话必须包含以下关键词之一：洗澡、狗狗
2. 不允许绕过这些关键词，给出通用夸奖
3. 必须针对"${userInput}"这件具体的事进行夸奖
```

**改进点：**
- ✅ 显式列出关键词（"洗澡、狗狗"）
- ✅ 明确要求"第一句话必须包含这些关键词"
- ✅ 用"不允许绕过"强化强制性

---

### 策略 2：在 System Prompt 中固定三句话结构

**V5.2 的做法（失败）：**
```
你的说话方式：
1. 先听懂 TA 的感受
2. 然后看见 TA 的努力
3. 最后给 TA 力量
```

**问题：**
- 只是"建议"AI 按照这个流程
- AI 可以"理解"但"不严格执行"

---

**V5.3 的做法（激进）：**
```
### 规则 2：三句话结构（固定格式）
第一句：回应具体事情（必须包含关键词）
  - 格式："${nickname}，[关键词相关的具体描述]"
  - 例如："${nickname}，给狗狗洗澡可不是件轻松事"

第二句：看见努力和品质
  - 从这件具体的事中，看到用户的品质

第三句：给予温暖和力量
  - 不讲道理，只给温暖
```

**改进点：**
- ✅ 用"固定格式"而不是"建议"
- ✅ 明确每一句话的内容和格式
- ✅ 给出具体的例子

---

### 策略 3：在 User Prompt 中重复强调格式

**V5.3 的做法：**
```
现在，请按照三句话结构，给出你的回应：
第一句：回应具体事情（必须包含关键词：洗澡、狗狗）
第二句：看见努力和品质
第三句：给予温暖和力量
```

**作用：**
- 在 User Prompt 的最后再次强调格式
- 确保 AI 在生成时"记住"这个要求
- 用"必须包含关键词"强化强制性

---

### 策略 4：实现简单的关键词提取函数

**代码实现：**
```typescript
function extractKeywords(input: string): string[] {
  const keywords: string[] = [];
  
  // 常见动词关键词
  const verbs = ["洗澡", "批评", "考试", "加班", "学习", ...];
  
  // 常见名词关键词
  const nouns = ["狗狗", "猫咪", "宠物", "领导", ...];
  
  // 检查输入中是否包含这些关键词
  for (const verb of verbs) {
    if (input.includes(verb)) {
      keywords.push(verb);
    }
  }
  
  // 如果没有提取到关键词，则将整个输入作为关键词
  if (keywords.length === 0) {
    keywords.push(input.substring(0, Math.min(10, input.length)));
  }
  
  return keywords;
}
```

**作用：**
- 自动提取用户输入中的关键词
- 确保关键词是具体的、可识别的
- 如果没有匹配到预定义关键词，则使用输入的前10个字符

---

## V5.3 vs V5.2 对比

### 场景：用户说"狗狗今天洗澡了，很可爱"

**V5.2 的 Prompt（失败）：**
```
${nickname} 对你说：
"狗狗今天洗澡了，很可爱"

**重要提醒：你必须在回应中明确提到"狗狗今天洗澡了，很可爱"里的具体事情。**
```

**V5.2 的回答（错误）：**
```
你对毛孩子的爱，它们都感受得到
```

**问题：**
- AI "理解"了要求，但"没有执行"
- 只是给了通用夸奖

---

**V5.3 的 Prompt（激进）：**
```
${nickname} 对你说：
"狗狗今天洗澡了，很可爱"

【关键词提取】
从用户的输入中，我们提取了以下关键词：洗澡、狗狗

【强制要求】
1. 你的第一句话必须包含以下关键词之一：洗澡、狗狗
2. 不允许绕过这些关键词，给出通用夸奖

现在，请按照三句话结构，给出你的回应：
第一句：回应具体事情（必须包含关键词：洗澡、狗狗）
第二句：看见努力和品质
第三句：给予温暖和力量
```

**V5.3 期望的回答（正确）：**
```
小美，给狗狗洗澡可不是件轻松事，从打湿毛发到吹干，每一步都需要耐心。
你能这么细心地照顾它，说明你是个有责任感的人。
看着它洗完澡后干净可爱的样子，是不是觉得所有辛苦都值得了？
```

**改进点：**
- ✅ 第一句话明确包含"洗澡"和"狗狗"关键词
- ✅ 具体描述了洗澡的过程
- ✅ 夸奖了"耐心"和"责任感"
- ✅ 最后呼应了"干净可爱"

---

## V5.3 的核心理念

### 从"提醒"到"强制"

| 维度 | V5.2（提醒） | V5.3（强制） |
|------|-------------|-------------|
| **Prompt 策略** | 提醒 AI 要回应具体内容 | 显式提取关键词，强制 AI 必须使用 |
| **格式要求** | 建议三步对话 | 固定三句话结构 |
| **关键词处理** | 隐式（AI 自己理解） | 显式（明确列出关键词） |
| **强制性** | 弱（AI 可以忽略） | 强（AI 必须遵守） |

### 为什么 V5.3 会更有效？

1. **显式关键词** - AI 不需要"理解"用户输入，只需要"使用"提供的关键词
2. **固定格式** - 减少 AI 的自由度，确保输出符合要求
3. **重复强调** - 在 System Prompt 和 User Prompt 中都强调格式
4. **降低温度** - 从 0.9 降到 0.85，确保更好地遵循指令

---

## 测试验证

### 关键测试场景

1. **狗狗洗澡** - 验证是否包含"洗澡"或"狗狗"关键词
2. **被领导批评** - 验证是否包含"批评"或"领导"关键词
3. **考试没考好** - 验证是否包含"考试"关键词
4. **加班到很晚** - 验证是否包含"加班"关键词

### 验证标准

**必须满足：**
- [ ] 第一句话必须包含提取的关键词
- [ ] 严格按照三句话结构输出
- [ ] 不能给出与关键词无关的通用夸奖

**加分项：**
- [ ] 具体描述了这件事的细节
- [ ] 夸奖了这件事背后的品质
- [ ] 最后呼应了用户的感受

---

## 如果 V5.3 还是失败怎么办？

### 备选方案 1：Few-Shot Learning

在 Prompt 中增加 3-5 个完整的示例对话：

```
示例 1：
用户输入："狗狗今天洗澡了"
关键词：洗澡、狗狗
回应："小美，给狗狗洗澡可不是件轻松事..."

示例 2：
用户输入："今天被批评了"
关键词：批评
回应："小美，被批评的那一刻，心里一定很难受..."
```

### 备选方案 2：使用 JSON 格式输出

要求 AI 输出 JSON 格式，强制包含关键词：

```json
{
  "keywords_used": ["洗澡", "狗狗"],
  "sentence1": "小美，给狗狗洗澡可不是件轻松事...",
  "sentence2": "你能这么细心地照顾它...",
  "sentence3": "看着它洗完澡后干净可爱的样子..."
}
```

### 备选方案 3：切换到更强的模型

如果 DeepSeek 的指令遵循能力不够，考虑切换到：
- GPT-4
- Claude 3.5
- 其他更强的模型

---

## 总结

**V5.3 的核心改进：从"提醒"升级到"强制"。**

不再依赖 AI 的"理解"，而是通过显式提取关键词、固定输出格式、重复强调要求，强制 AI 必须回应用户的具体输入内容。

**如果 V5.3 还是失败，说明问题不在 Prompt，而在模型本身的指令遵循能力。**
