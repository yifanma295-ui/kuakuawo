# "夸夸我" App V4.0 优化需求文档

## 概述

V4.0 版本聚焦于**主题切换体验优化**、**分享与社交功能**、**Echo 页面增强**和**Onboarding 流程完善**，旨在提升用户的交互体验和内容传播能力。

---

## 需求详情

### 需求 1：主题切换后默认展示圆环设计

**优先级**：高  
**预计工作量**：2-3 小时

#### 问题描述
用户在主题页面选择了不同主题后，返回首页时，应该默认展示圆环按钮，而不是显示之前生成的夸奖文案。

#### 具体需求
- 用户修改主题后，首页的状态应该重置
- 圆环按钮应该始终作为首页的主要交互元素
- 之前生成的文案可以通过"换一句"功能重新生成

#### 设计建议
```
首页流程：
1. 用户进入首页 → 显示圆环按钮
2. 用户点击圆环 → 生成夸奖文案
3. 用户修改主题 → 返回首页 → 重置为圆环状态
4. 用户点击圆环 → 使用新主题生成文案
```

#### 实现框架
```typescript
// 在 app-context 中添加主题变化监听
useEffect(() => {
  if (selectedTheme !== previousTheme) {
    // 重置首页状态
    setPraise(null);
    setShowPraise(false);
  }
}, [selectedTheme]);
```

#### 验证方式
1. 进入应用，生成一句夸奖
2. 进入主题页面，选择不同主题
3. 返回首页，确认显示圆环按钮
4. 点击圆环，确认使用新主题生成文案

---

### 需求 2：优化分享功能与 Echo 回响记录

**优先级**：高  
**预计工作量**：4-5 小时

#### 问题描述
当用户生成夸奖文案后，需要提供更便捷的分享和复制功能，同时在 Echo 页面记录用户的分享行为。

#### 具体需求

**2.1 修改收藏按钮文案**
- 将"收藏高光时刻"改为"收藏高光"
- 保持原有的收藏功能不变

**2.2 新增分享按钮**
- 按钮文案："分享共鸣"
- 按钮 Icon：分享图标（share-2 或类似）
- 功能：点击后直接复制夸奖文案到剪贴板
- 用户反馈：显示"已复制到剪贴板"的 Toast 提示

**2.3 Echo 页面回响记录**
- 用户点击"分享共鸣"时，在 Echo 页面的"回响"分类中新增一条记录
- 回响记录包含：
  - 用户复制的夸奖文案内容
  - 复制时间戳
  - 来源主题
- 设计上与"共鸣"分类保持一致

#### 设计建议

**首页夸奖卡片布局**：
```
┌─────────────────────────┐
│  夸奖文案内容           │
│                         │
├─────────────────────────┤
│ [收藏高光] [分享共鸣]   │
│ [换一句]                │
└─────────────────────────┘
```

**Echo 页面回响分类**：
```
回响（用户分享的夸奖）
├─ 分享时间
├─ 夸奖文案内容
├─ 来源主题标签
└─ 分享次数统计
```

#### 实现框架

```typescript
// 分享功能
const handleShare = async (praise: string) => {
  try {
    await Clipboard.setStringAsync(praise);
    showToast("已复制到剪贴板");
    
    // 记录到 Echo 页面
    await recordEchoShare({
      content: praise,
      theme: selectedTheme,
      timestamp: new Date(),
    });
  } catch (error) {
    showToast("复制失败，请重试");
  }
};

// Echo 页面数据结构
interface EchoShare {
  id: string;
  content: string;
  theme: string;
  timestamp: Date;
  shareCount: number;
}
```

#### 验证方式
1. 生成一句夸奖文案
2. 点击"分享共鸣"按钮
3. 确认显示"已复制到剪贴板"提示
4. 进入 Echo 页面，确认在"回响"分类中看到新增的分享记录
5. 验证分享记录包含正确的文案内容和主题信息

---

### 需求 3：分享 Echo 界面

**优先级**：中  
**预计工作量**：3-4 小时

#### 问题描述
用户可能想要分享自己的 Echo 页面（收藏的夸奖和回响记录）给朋友，需要提供分享功能。

#### 具体需求
- 在 Echo 页面顶部添加"分享"按钮
- 点击后生成分享链接或分享卡片
- 支持分享到微信、微博、小红书等社交平台
- 分享内容包含：用户昵称、收藏数、回响数、部分精选夸奖

#### 设计建议

**分享卡片内容**：
```
┌──────────────────────────┐
│  🌸 [用户昵称]的夸夸我   │
│                          │
│  共鸣：XX 条             │
│  回响：XX 条             │
│                          │
│  精选夸奖：              │
│  "今天的你闪闪发光"      │
│  "坚持就是最好的礼物"    │
│                          │
│  点击链接查看完整内容    │
└──────────────────────────┘
```

#### 实现框架
```typescript
// 生成分享链接
const generateShareLink = (userId: string) => {
  return `${APP_URL}/share/${userId}`;
};

// 分享卡片生成
const generateShareCard = async (echoData: EchoData) => {
  // 使用 canvas 或图片库生成分享卡片
  const card = await generateImage({
    nickname: echoData.nickname,
    resonanceCount: echoData.resonanceCount,
    shareCount: echoData.shareCount,
    selectedPraises: echoData.selectedPraises.slice(0, 3),
  });
  return card;
};
```

#### 验证方式
1. 进入 Echo 页面
2. 点击分享按钮
3. 确认生成分享链接或分享卡片
4. 验证分享内容的准确性
5. 测试分享到不同平台

---

### 需求 4：优化 Onboarding 流程

**优先级**：高  
**预计工作量**：2-3 小时

#### 问题描述
当前 Onboarding 页面每次进入应用都会显示，应该只在用户首次使用时展示，后续进入应该直接跳过。

#### 具体需求
- 用户首次打开应用时显示 Onboarding 页面
- 用户完成 Onboarding（输入昵称并点击"开始我的夸夸之旅"）后，标记为已完成
- 后续每次打开应用都直接进入首页，不再显示 Onboarding
- 用户可以在设置页面重新触发 Onboarding（可选功能）

#### 设计建议
```
应用启动流程：
1. 检查 localStorage 中的 onboardingComplete 标志
2. 如果为 false → 显示 Onboarding 页面
3. 如果为 true → 直接进入首页（Tab 导航）
4. 用户完成 Onboarding → 设置 onboardingComplete = true
```

#### 实现框架
```typescript
// 在 app/_layout.tsx 中
const [onboardingComplete, setOnboardingComplete] = useState(false);

useEffect(() => {
  // 检查本地存储
  const checkOnboarding = async () => {
    const completed = await AsyncStorage.getItem("onboardingComplete");
    setOnboardingComplete(completed === "true");
  };
  checkOnboarding();
}, []);

// 路由逻辑
if (!onboardingComplete) {
  return <Onboarding onComplete={() => setOnboardingComplete(true)} />;
}

return <TabNavigator />;
```

#### 验证方式
1. 清除应用数据，首次打开应用
2. 确认显示 Onboarding 页面
3. 完成 Onboarding（输入昵称）
4. 确认进入首页
5. 关闭应用，重新打开
6. 确认直接进入首页，不显示 Onboarding

---

## 优先级排序与实现建议

| 需求 | 优先级 | 预计工作量 | 依赖关系 | 建议实现顺序 |
|------|--------|----------|---------|-----------|
| 需求 4 | 高 | 2-3h | 无 | 1（基础功能） |
| 需求 1 | 高 | 2-3h | 无 | 2（核心体验） |
| 需求 2 | 高 | 4-5h | 需求 1 | 3（社交功能） |
| 需求 3 | 中 | 3-4h | 需求 2 | 4（分享功能） |

**建议实现路线**：
1. 先完成 Onboarding 流程优化（需求 4）
2. 再优化主题切换体验（需求 1）
3. 然后增强分享与 Echo 功能（需求 2）
4. 最后实现 Echo 页面分享（需求 3）

---

## 数据库变更

### 新增字段

**echo_shares 表**（新增）
```sql
CREATE TABLE echo_shares (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  content TEXT NOT NULL,
  theme VARCHAR(50),
  timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  share_count INT DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 修改字段

**echo_praises 表**
- 添加 `share_count` 字段（用于统计分享次数）

---

## 测试检查清单

- [ ] Onboarding 仅在首次显示
- [ ] 主题切换后首页重置为圆环状态
- [ ] 分享按钮正确复制文案到剪贴板
- [ ] Echo 页面正确记录分享记录
- [ ] Echo 分享链接可正确生成
- [ ] 分享卡片内容准确
- [ ] 所有平台分享功能正常

---

## 后续优化方向

1. **添加分享统计**：追踪每条夸奖被分享的次数，显示"最受欢迎的夸奖"
2. **实现用户排行榜**：基于分享数和收藏数，展示"最活跃的夸夸者"
3. **添加评论功能**：用户可以对其他用户的 Echo 页面进行评论和点赞
4. **实现推荐算法**：根据用户行为推荐相似的主题或夸奖文案
